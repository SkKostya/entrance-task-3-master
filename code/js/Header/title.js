import React from "react";

const Title = (props) => {
	return (
		<p className={props.title}>
			<strong><span style={{color: '#ff0000'}}>Я</span>ндекс</strong> Переговорки
		</p>
	);
};

export default Title;