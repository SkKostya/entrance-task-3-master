import React, { Component } from "react";

import { Header, Main } from "../../components";

const Wrapper = () => {
	return (
		<div>
			<Header />
			<Main />
		</div>
	);
};

export default Wrapper;